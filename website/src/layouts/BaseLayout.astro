---
import '@fontsource-variable/inter/wght.css';
import '@fontsource-variable/inter/wght-italic.css';
import '../styles/global.scss';

interface Props {
  title?: string;
  description?: string;
  markdownPath?: string;
}

const {
  title,
  description = 'Knap is a safe, flexible templating language for turning structured data into Markdown.',
  markdownPath,
} = Astro.props;
const pageTitle = title ? `${title} · Knap` : 'Knap · The template language for Markdown';
const canonicalPath = Astro.url.pathname.replace(/\/index\.html$/, '/').replace(/\.html$/, '');
const canonical = new URL(canonicalPath, Astro.site);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    {markdownPath && <link rel="alternate" type="text/markdown" href={markdownPath} title={`${title ?? 'Knap'} as Markdown`} />}
    <meta property="og:type" content="website" />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <title>{pageTitle}</title>
    <style is:inline>
      a { color: inherit; text-decoration: none; }
      button.header-action { appearance: none; border: 1px solid transparent; outline: 0; background: transparent; color: inherit; font: inherit; }
    </style>
  </head>
  <body><slot /></body>
</html>

<script>
  import '../scripts/site';
</script>
